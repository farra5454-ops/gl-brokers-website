GL BROKERS - 9 FILES THAT DID NOT UPLOAD
=========================================

Your last GitHub upload was missing these 9 files. Everything else
made it fine - 32 of 40 pages, robots.txt, CSS and JS all verified
live and correct.

WHAT TO DO
----------
1. Go to your repo on github.com
2. Click "Add file"  ->  "Upload files"
3. Drag in BOTH items from this folder:
       - the whole  gl-contractor-class-codes  FOLDER
       - the file   sitemap.xml
4. Commit. Cloudflare Pages redeploys on its own, about 1-2 minutes.

Drag the FOLDER itself, not the 8 loose files inside it. That is what
keeps the /gl-contractor-class-codes/ path correct in the repo.


WHAT THESE FILES DO
-------------------
The 8 pages in gl-contractor-class-codes each gained a "Coverage
Across Florida" block linking to all 8 GL city pages. That lifts every
city page from 2-3 internal links to 11 - which is the entire point of
the fix, because the pages Google DID index all had 11.

sitemap.xml carries refreshed lastmod dates on the 17 pages that
changed. That is the signal that prompts Google to recrawl them sooner.

Right now the workers' comp half of this fix is live and working.
The general liability half is not. These 9 files close that gap.
